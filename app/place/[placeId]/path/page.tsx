export default function Place({ params: { id } }: { params: { id: string } }) {
  return (
    <div>
      /place/:id/route
      <button>역사가 타입~</button>
      <button>난 바빠~</button>
    </div>
  );
}
